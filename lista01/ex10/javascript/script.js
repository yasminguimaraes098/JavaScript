var numero = parseInt(prompt("Digite um número inteiro positivo que seja maior que 0"));

var nome = prompt("Digite seu nome completo");

for (var i = 0; i < numero; i++) {
    document.write(`${nome}</br>`);
}